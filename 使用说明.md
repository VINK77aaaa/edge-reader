# Edge 阅读器

一个本地小工具：把任意文字粘贴进去，用舒服的排版阅读，并用 Windows 自带的语音引擎（也就是 Edge「大声朗读」用的同一套 SAPI 引擎）读出来。

## 使用方法

双击 **启动阅读器.bat** 即可（首次运行会自动安装 pywebview 和 pywin32 两个依赖）。

### 功能

- **新建阅读**：粘贴文字，可选填标题，点「开始阅读」。
  - 勾选「智能整理段落」可自动合并从网页 / PDF 复制时被硬换行打断的句子。
- **沉浸式阅读**：
  - A＋ / A－ 调字号，可调行距（紧凑 / 标准 / 宽松）；
  - 三种背景主题：浅色 / 米黄 / 深色；
  - 这些设置会记住，下次打开还是你习惯的样式。
- **语音朗读**（底部悬浮条）：
  - ▶ 开始朗读，朗读中变成 ⏸ 可暂停/继续，■ 停止；
  - 可调语速、选择语音，点击任意段落可从那一段开始朗读；正在朗读的段落会自动高亮并跟随滚动；
  - **Edge 在线语音**（默认，需联网）：晓晓（女）、云健（男）、云希、云扬、晓伊，即 Edge「大声朗读」同款神经网络语音，音质自然；合成结果会缓存，同样的内容第二次朗读是秒出；
  - **本机语音**（离线）：Microsoft Huihui / Zira 等，不联网也能用，音质偏机械。
- **历史记录**：粘贴过的文章按标题保存（默认折叠，点击标题展开），随时点开重看；支持删除。
- **继续上次阅读**：回到上次没读完的文章，并恢复到当时的阅读位置。
- **文档导入**：把 docx / doc / txt / md 文件拖进窗口（或点「📂 打开文档」）即可加载正文阅读朗读；老式 .doc 需要电脑装有 Word。
- **快捷键**（阅读页）：`空格` 播放/暂停，`Esc` 返回，`←`/`→` 上一句/下一句，`F11` 全屏/退出全屏。
- **朗读条**：显示合成/播放进度（如「合成中 3/25」）；朗读中切换语音会自动从当前段落用新语音重启。

## 数据保存在哪

文章和设置保存在 `C:\Users\你的用户名\AppData\Roaming\EdgeReader\articles.json`，不联网、不上传。

## 手动安装依赖（如果 bat 出问题）

```
py -m pip install pywebview pywin32
py app.py
```

需要 Windows 10/11 自带的 WebView2 运行时（装了 Edge 就有）和 Python 3.9+。
